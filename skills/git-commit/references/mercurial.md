# Mercurial commands

Load when the repo root has `.hg`. Same staging, message, and confirmation
rules as git — only the commands differ.

```bash
hg status                 # survey
hg diff                   # survey
hg add <paths>            # stage untracked files
hg commit -m "<message>"  # commit
```
